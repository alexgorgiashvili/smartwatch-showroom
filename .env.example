APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

OPENAI_API_KEY=sk-
OPENAI_ORG_ID=org-
OPENAI_MODEL=gpt-4-turbo
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_BASE_URL=https://api.openai.com/v1

# AI Mode: 'professional' (default) or 'sarcastic' (for fun/testing)
AI_MODE=professional

PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=products

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

#========================================
# Security Configuration
#========================================
# Webhook signature verification timeout (in seconds, default: 300 = 5 minutes)
WEBHOOK_SIGNATURE_TIMEOUT=300

# Webhook rate limiting (max messages per minute per customer)
WEBHOOK_RATE_LIMIT=100

# Maximum message content length (in characters)
MAX_MESSAGE_LENGTH=5000

# Rate limiting for different endpoints (requests per minute)
WEBHOOK_RECEPTION_RATE_LIMIT=1000
MESSAGE_SEND_RATE_LIMIT=30
AI_SUGGESTION_RATE_LIMIT=10
SEARCH_RATE_LIMIT=60
GET_REQUESTS_RATE_LIMIT=200
POST_REQUESTS_RATE_LIMIT=100

# Data encryption
ENCRYPT_MESSAGE_CONTENT=false

# Logging
LOG_WEBHOOK_PAYLOADS=false
LOG_SENSITIVE_DATA=false

# Data retention policies (in days)
MESSAGE_RETENTION_DAYS=90
AUDIT_LOG_RETENTION_DAYS=180
WEBHOOK_LOG_RETENTION_DAYS=30
